# Spotify OAuth client ID (public identifier, not a secret)
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Auth flow:
# - server: self-host server uses /api/auth/start and /api/auth/callback
# - client: local simulator/browser uses /callback.html
VITE_SPOTIFY_AUTH_MODE=server
